{
  "name": "vm-configurer",
  "version": "1.0.0",
  "description": "The vm configurer is a component in the whole we.learning platform responsible for initializing the workspace on the virtual machine depending on the user's requirement",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node output/index.js",
    "dev": "nodemon --watch 'src/**/*.ts' --exec 'ts-node' src/index.ts",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix"
  },
  "repository": "https://github.com/dtn1999/codercm",
  "author": "dtn1999",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@hapi/hapi": "^21.2.1",
    "@trpc/server": "^10.11.0",
    "@typescript-eslint/eslint-plugin": "^5.51.0",
    "@typescript-eslint/parser": "^5.51.0",
    "eslint": "^8.34.0",
    "eslint-config-prettier": "^8.6.0",
    "eslint-plugin-prettier": "^4.2.1",
    "prettier": "^2.8.4"
  },
  "devDependencies": {
    "@types/hapi__hapi": "^21.0.0",
    "@types/node": "^18.13.0",
    "nodemon": "^2.0.20",
    "ts-node": "^10.9.1",
    "typescript": "^4.9.5"
  }
}
